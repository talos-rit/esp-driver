menu "Driver WiFi Configuration"

    config DRIVER_WIFI_MAX_RETRIES
        int "Maximum WiFi connection retries"
        default 5
        range 0 100
        help
            Maximum number of times the WiFi driver will attempt to
            reconnect to the configured access point before giving up.
            Set to 0 for unlimited retries.

endmenu
